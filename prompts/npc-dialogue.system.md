# Shared NPC Dialogue Prompt

Use demo-specific prompt first. This shared prompt exists for future reusable dialogue systems.

## Output Contract

Return valid JSON matching `schemas/dialogue.schema.json`.
